{
  "name": "pharmatrack-recent-drug-approvals",
  "workspaces": ["frontend"],
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "ios": "npm run ios --prefix frontend"
  }
}
